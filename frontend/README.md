## ScholarChain Frontend

Run:

```bash
npm install
npm run dev
```

Environment (`frontend/.env.local`):

- `NEXT_PUBLIC_RPC_URL`
- `NEXT_PUBLIC_SCHOLARCHAIN_ADDRESS`
- `NEXT_PUBLIC_BACKEND_URL`

Pages:

- `/register`, `/login`
- `/dashboard`, `/students`, `/allocate`, `/audit`
- `/donate`
